<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="event-scheduler-1" author="frolov" context="prod or dev">
        <sql>
            CREATE EVENT delete_test
            ON SCHEDULE EVERY 1 DAY
            STARTS TIMESTAMP(CURRENT_DATE)
            DO DELETE FROM TEST WHERE DATEDIFF(NOW(), DATE_ADDED) >= 30;
        </sql>
    </changeSet>

    <changeSet id="event-scheduler-2" author="frolov" context="prod or dev">
        <sql>
            CREATE EVENT delete_test_execution
            ON SCHEDULE EVERY 1 DAY
            STARTS TIMESTAMP(CURRENT_DATE)
            DO DELETE FROM TEST_EXECUTION WHERE DATEDIFF(NOW(), END_TIME) >= 30;
        </sql>
    </changeSet>

</databaseChangeLog>