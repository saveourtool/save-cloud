package com.saveourtool.save.agent

/**
 * Env names which agent supports and expects
 */
enum class AgentEnvName {
    AGENT_ID,
    AGENT_NAME,
    AGENT_VERSION,
    CLI_COMMAND,
    DEBUG,
    EXECUTION_ID,
    HEARTBEAT_URL,
    TEST_SUITES_DIR,
    ;
}
